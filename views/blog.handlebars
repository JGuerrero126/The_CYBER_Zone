<div data-id="{{id}}" class="card custom-card">
  <h5 class="card-header custom-header">{{title}}</h5>
  <div class="card-body custom-body">
    <p class="card-text custom-body">{{text}}</p>
    <p>Posted by {{name}} on {{format_date date_created}}</p>
  </div>
</div>

{{#if comments}}
  {{#each comments as |comment|}}
    <div class="card comment-card">
      <div class="card-header custom-header">
      </div>
      <div class="card-body">
        <blockquote class="blockquote mb-0">
          <p>{{comment.text}}</p>
          <footer class="blockquote-footer">{{name}}
            on
            {{format_date comment.date_created}}</footer>
        </blockquote>
      </div>
    </div>
  {{/each}}
{{else}}
  <p>No comments here, you could add one though!</p>
{{/if}}

<form data-id="{{id}}" class="newcomment">
  <div class="mb-3">
    <label for="comment-text" class="form-label">Comment:</label>
    <input
      type="text"
      class="form-control"
      id="comment-text"
      aria-describedby="commentinput"
    />
  </div>
  <button type="submit" class="btn btn-primary" data-id="{{id}}">Submit</button>
</form>

<script src="/js/blog.js"></script>